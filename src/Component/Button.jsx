
const Button = ({name} ) => {
  return (
    <div>
      <button className=" bg-slate-200 px-4 py-2 mr-5 mt-6 mb-8 rounded-md whitespace-nowrap">{name}</button>
    </div>
  )
}

export default Button;
